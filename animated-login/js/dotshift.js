/* ==========================================
   LEFT PANEL BACKGROUND
   Simple image background, no WebGL needed
   ========================================== */

export function initDotShift() {
    // No longer needed - background is handled via CSS
}
